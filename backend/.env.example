# Backend Environment Configuration

# =============================================================================
# LLM Provider Configuration (Dual-Provider System with Automatic Fallback)
# =============================================================================

# Gemini API Key (CRITICAL: Keep this secret, never expose to frontend)
GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic API Key (Optional: For Claude fallback support)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM Model Selection (Verified stable models)
GEMINI_MODEL=gemini-2.5-flash
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# LLM Provider Priority
# Primary provider to use first (gemini or anthropic)
LLM_PRIMARY=gemini
# Fallback provider if primary fails with 429/quota/timeout errors
LLM_FALLBACK=anthropic
# Force use of fallback provider (useful for testing)
FORCE_FALLBACK=false

# =============================================================================
# Authentication & Security
# =============================================================================

# JWT Secret Key (Generate with: openssl rand -hex 32)
# Example: a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2
JWT_SECRET_KEY=your_jwt_secret_key_here_generate_with_openssl_rand_hex_32

# JWT Algorithm
JWT_ALGORITHM=HS256

# JWT Token Expiration (in minutes) - 30 minutes recommended for security
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Origins (comma-separated frontend URLs)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100
AI_RATE_LIMIT_PER_MINUTE=10

# Application Settings
APP_NAME=La Consulta Backend
APP_VERSION=1.0.0
# DEBUG should be False in production for security
DEBUG=False
